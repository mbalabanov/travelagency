{% extends 'base.html.twig' %}

{% block title %}Edit Journey{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <h1>Edit Journey</h1>
            {{ include('journey/_form.html.twig', {'button_label': 'Update'}) }}
            <a href="{{ path('journey_index') }}" class="btn btn-primary m-2">Back to List</a>
            {{ include('journey/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
