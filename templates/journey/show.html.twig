{% extends 'base.html.twig' %}

{% block title %}Journey{% endblock %}

{% block body %}
    <h1>Journey</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ journey.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ journey.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ journey.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ journey.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ journey.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('journey_index') }}">back to list</a>

    <a href="{{ path('journey_edit', {'id': journey.id}) }}">edit</a>

    {{ include('journey/_delete_form.html.twig') }}
{% endblock %}
